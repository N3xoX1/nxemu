# discord-rpc

Vendored from https://github.com/discord/discord-rpc (MIT).

Only the library sources and headers are kept. RapidJSON headers (needed by
discord-rpc serialization) live under `thirdparty/rapidjson`.
